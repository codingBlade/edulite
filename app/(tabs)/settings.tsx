import { Text, View } from 'react-native';

export default function Settings() {
  return (
    <View
      style={{
        flex: 1,
        width: 'auto',
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: '#acacac',
      }}
    >
      <Text>Settings is here</Text>
    </View>
  );
}
